[pytest]
DJANGO_SETTINGS_MODULE = awesome_repos.test_settings
python_files = tests.py test_*.py *_tests.py
addopts = --strict-markers --durations=20
markers =
    slow: tests that are intentionally split from the fast path when needed
filterwarnings = ignore::DeprecationWarning:pkg_resources
